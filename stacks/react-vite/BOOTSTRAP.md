# React + Vite — bootstrap

1. Vite React TS in `frontend.appDir`.
2. Pins from `packages.example.yaml`; Tailwind.
3. Dev proxy `/api` → backend.
4. `.env.example`: `VITE_API_URL`
5. Install with `pnpm`.
