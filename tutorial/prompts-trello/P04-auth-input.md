# P04 — Auth input layer

```
Use prompts/generate-input-layer.md.
Feature: docs/features/auth.md
Adapter: stacks/<backend.framework>/CONVENTION.md
Create input/validation layer only (DTO / FormRequest / Serializer).
List files you will create, then write them. No service/HTTP yet.
```
