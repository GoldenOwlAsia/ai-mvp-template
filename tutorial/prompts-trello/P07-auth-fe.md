# P07 — Auth frontend (skip if frontend.framework is none)

```
Use prompts/generate-fe-page.md.
Follow stacks/react-vite/CONVENTION.md.
Build login + register UI and API client: access token in memory,
refresh via httpOnly cookie with credentials. Loading and error states required.
```
