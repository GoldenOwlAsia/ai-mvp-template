# P03 — Bootstrap apps

```
Use prompts/bootstrap-from-config.md.
Read stacks/<backend.framework>/BOOTSTRAP.md and FE adapter if frontend.framework is not none.
Scaffold apps into backend.appDir / frontend.appDir with exact package pins from stack.config.
Skeleton + install only — no full TaskFlow features yet.
Report checklist + how to run /health (and FE dev server if applicable) locally.
Do not invent secrets; use .env.example only.
```
