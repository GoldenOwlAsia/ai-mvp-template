# P06 — Auth HTTP layer

```
Use prompts/generate-controller.md.
Wire routes under /api/v1/auth (register, login, refresh, logout, me).
PATCH not PUT. Error envelope: statusCode, code, message, timestamp, path.
Register module/routes per active adapter CONVENTION.md.
```
