# P22 — Security review

```
Use prompts/review-security.md.
Check: soft-delete everywhere, no password/token leakage, cookie flags,
deps ⊆ stack.config.yaml, authz on project/task/comment.
Findings only (severity + file). No drive-by refactors.
```
