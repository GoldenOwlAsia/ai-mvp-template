# P10 — Project service

```
Use prompts/generate-service.md.
Feature: docs/features/project.md
Owner-scoped list/create/get/patch/soft-delete.
Always filter deletedAt (or SoftDeletes). PROJECT_NOT_FOUND when not owner / missing.
No HTTP yet.
```
