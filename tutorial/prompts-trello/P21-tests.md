# P21 — Tests

```
Use prompts/write-test.md.
Map GIVEN/WHEN/THEN from docs/features auth, project, task (and comment if implemented)
to unit/e2e tests per adapter test stack in stack.config tooling.test.
Do not change production code unless a failing test proves a bug — then report before fixing.
```
