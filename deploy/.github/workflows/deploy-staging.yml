# Skeleton — wire provider CLIs as GitHub Actions secrets before enabling
# Providers come from stack.config.yaml (deploy.web / deploy.api) — do not hardcode Railway.
name: deploy-staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - name: Stub — configure deploy
        run: |
          echo "Wire CLIs for providers in stack.config.yaml (e.g. Vercel + Render or Railway)."
          echo "Do not print secret values."
          echo "Require human approval for production (separate workflow)."
      # Example placeholders (do not enable until secrets exist):
      # - run: vercel deploy --prebuilt --token=$VERCEL_TOKEN
      # - run: # Render / Railway deploy per active deploy.api
